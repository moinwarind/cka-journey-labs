apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-app
    env: dev
    tier: frontend
    version: v1
  name: web-svc
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: web-app
  type: NodePort
status:
  loadBalancer: {}
