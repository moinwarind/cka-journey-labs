apiVersion: batch/v1
kind: CronJob
metadata:
  name: weekly-audit
spec:
  jobTemplate:
    metadata:
      name: weekly-audit
    spec:
      template:
        metadata: {}
        spec:
          containers:
          - image: busybox
            name: weekly-audit
            resources: {}
          restartPolicy: OnFailure
  schedule: 0 6 * * 1
status: {}
