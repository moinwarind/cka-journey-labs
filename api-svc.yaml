apiVersion: v1
kind: Service
metadata:
  labels:
    app: api-server
  name: api-server
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 9000
  selector:
    app: api-server
status:
  loadBalancer: {}
