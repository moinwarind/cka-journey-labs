apiVersion: v1
kind: Service
metadata:
  labels:
    app: gp-deploy
  name: gp-deploy
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: gp-deploy
status:
  loadBalancer: {}
