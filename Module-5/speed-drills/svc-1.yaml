apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-frontend
  name: web-frontend
spec:
  ports:
  - port: 8080
    protocol: TCP
    targetPort: 80
  selector:
    app: web-frontend
status:
  loadBalancer: {}
