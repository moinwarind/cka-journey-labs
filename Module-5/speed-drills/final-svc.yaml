apiVersion: v1
kind: Service
metadata:
  labels:
    app: final-deploy
  name: final-deploy
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 8080
  selector:
    app: final-deploy
status:
  loadBalancer: {}
