apiVersion: batch/v1
kind: CronJob
metadata:
  name: c1
spec:
  jobTemplate:
    metadata:
      name: c1
    spec:
      template:
        metadata: {}
        spec:
          containers:
          - image: busybox
            name: c1
            resources: {}
          restartPolicy: OnFailure
  schedule: '*/5 * * * *'
status: {}
