apiVersion: v1
kind: Service
metadata:
  labels:
    app: web-frontend
  name: web-frontend
spec:
  ports:
  - port: 3000
    protocol: TCP
    targetPort: 3000
  selector:
    app: web-frontend
status:
  loadBalancer: {}
