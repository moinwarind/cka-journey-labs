apiVersion: batch/v1
kind: CronJob
metadata:
  name: final-cron
spec:
  jobTemplate:
    metadata:
      name: final-cron
    spec:
      template:
        metadata: {}
        spec:
          containers:
          - image: busybox
            name: final-cron
            resources: {}
          restartPolicy: OnFailure
  schedule: 0 0 * * 1
status: {}
