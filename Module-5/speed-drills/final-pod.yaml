apiVersion: v1
kind: Pod
metadata:
  labels:
    run: final-pod
  name: final-pod
spec:
  containers:
  - image: busybox
    name: final-pod
    resources: {}
  dnsPolicy: ClusterFirst
  restartPolicy: Always
status: {}
