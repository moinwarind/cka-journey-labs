apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: front-line
  name: front-line
spec:
  replicas: 5
  selector:
    matchLabels:
      app: front-line
  strategy: {}
  template:
    metadata:
      labels:
        app: front-line
    spec:
      containers:
      - image: nginx:1.27
        name: nginx
        resources: {}
status: {}
