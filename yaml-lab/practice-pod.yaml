apiVersion: v1
kind: Pod
metadata: 
  name: practice-pod 
  labels:
    app: practice 
    env: lab    
spec:
  containers:
    - name: web
      image: nginx
      ports:
        - containerPort: 80
